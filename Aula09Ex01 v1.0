#include <stdio.h>
#include <stdlib.h>


int main (){
int v[5]={2,56,14,7,98};
int n, soma;
n = 0;
soma = 0;

    for(n=0 ; n < 5 ; n++){
       soma = soma + v[n];
    }

printf("a soma e %d", soma);
return 0;
}
